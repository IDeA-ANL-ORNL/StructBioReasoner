# StructBioReasoner Core Dependencies
# Python 3.8+ required

# AI and OpenAI Integration
openai>=1.0.0

# Core Scientific Computing
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0
matplotlib>=3.5.0

# Structural Biology
biopython>=1.79
# pymol-open-source>=2.5.0  # Install via: pip install pymol-open-source
# Note: PyMOL works best when installed via Homebrew: brew install pymol

# Molecular Dynamics
openmm[cuda12]>=8.0.0  # Install via: conda install -c conda-forge openmm
mdtraj>=1.9.0  # Install via: conda install -c conda-forge mdtraj
git+https://github.com/openmm/pdbfixer.git
#pdbfixer  # Install via: conda install -c conda-forge pdbfixer
# Note: OpenMM works best when installed via conda due to CUDA dependencies

# Machine Learning and Deep Learning
#torch>=1.12.0  # For ESM and other ML models
#torchvision>=0.13.0
fair-esm>=2.0.0  # ESM protein language models
biotite>=0.37.0  # For protein structure analysis

# RFDiffusion dependencies (optional - requires separate installation)
# hydra-core>=1.2.0
# omegaconf>=2.2.0
# Note: RFDiffusion requires separate installation from GitHub

# Rosetta dependencies (optional - requires license and separate installation)
# Note: Rosetta requires separate installation and academic/commercial license

# AlphaFold dependencies (optional - requires separate installation)
# tensorflow>=2.9.0
# dm-haiku>=0.0.9
# dm-tree>=0.1.7
# immutabledict>=2.2.1
# ml-collections>=0.1.1
# chex>=0.1.5
# Note: AlphaFold requires separate installation and large database downloads

# Additional scientific computing
scikit-learn>=1.1.0
seaborn>=0.12.0

# Async and Web
aiohttp>=3.8.0
requests>=2.25.0

# Configuration and Environment
pyyaml>=6.0
python-dotenv>=0.19.0

# Database
sqlalchemy>=1.4.0

# CLI and Progress
click>=8.0.0
rich>=12.0.0
tqdm>=4.62.0

# Optional Development Dependencies
# pytest>=7.0.0
# black>=23.0.0
# jupyter>=1.0.0

# Installation Notes:
# 1. PyMOL: Install via Homebrew for best results: brew install pymol
# 2. OpenAI API: Set OPENAI_API_KEY in .env file
# 3. Python 3.8+ required for async features
