# BindCraft Test

Ensures the BindCraft component is working as expected
by running a single 'computational_design' task.

## Running the Test

Run the test by calling the Python script on a compute node: `python run-bindcraft-agent.py`.

> The code is currently hard-coded to run on a machine with a single NVIDIA GPU.
